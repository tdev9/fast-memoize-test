{
  "name": "fast-memoize-test",
  "version": "1.0.0",
  "description": "test of fast-memoize",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tdev",
  "license": "MIT",
  "dependencies": {
    "fast-memoize": "^2.5.1"
  },
  "devDependencies": {
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  }
}
